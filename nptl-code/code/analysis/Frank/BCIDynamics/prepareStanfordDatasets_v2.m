%%
datasets(1).filename = 'R_2016-02-02_1';
datasets(1).task = '3ring';
datasets(1).subject = 'Jenkins';
datasets(1).savetags = 1;
datasets(1).controlType = 'arm';
datasets(1).arrayNames = {'M1','PMd'};

datasets(2).filename = 'J_2015-04-14';
datasets(2).task = 'CursorJump';
datasets(2).subject = 'Jenkins';
datasets(2).savetags = [3];
datasets(2).controlType = 'arm';
datasets(2).arrayNames = {'M1','PMd'};

datasets(3).filename = 'L_2015-06-05';
datasets(3).task = 'CursorJump';
datasets(3).subject = 'Larry';
datasets(3).savetags = [9];
datasets(3).controlType = 'arm';
datasets(3).arrayNames = {'M1'};

datasets(4).filename = 'J_2015-01-20';
datasets(4).task = 'CursorJump';
datasets(4).subject = 'Jenkins';
datasets(4).savetags = [10];
datasets(4).controlType = 'bci';
datasets(4).arrayNames = {'M1','PMd'};

datasets(5).filename = 'L_2015-01-14';
datasets(5).task = 'CursorJump';
datasets(5).subject = 'Larry';
datasets(5).savetags = [9];
datasets(5).controlType = 'bci';
datasets(5).arrayNames = {'M1'};

datasets(6).filename = 'J_2014-09-10';
datasets(6).task = 'gain';
datasets(6).subject = 'Jenkins';
datasets(6).savetags = [];
datasets(6).controlType = 'arm';
datasets(6).arrayNames = {'M1','PMd'};

datasets(7).filename = 't5.2017.09.20';
datasets(7).task = 'BG_2DCursor';
datasets(7).subject = 'T5';
datasets(7).savetags = [5 6 9 10 11 12];
datasets(7).controlType = 'bci';
datasets(7).arrayNames = {'M1a','M1b'};

datasets(8).filename = 'R_2017-10-04_1';
datasets(8).task = 'bci_vs_arm';
datasets(8).subject = 'Jenkins';
datasets(8).savetags = [14];
datasets(8).controlType = 'arm';
datasets(8).arrayNames = {'M1','PMd'};

datasets(9).filename = 'R_2017-10-04_1';
datasets(9).task = 'bci_vs_arm';
datasets(9).subject = 'Jenkins';
datasets(9).savetags = [12];
datasets(9).controlType = 'bci';
datasets(9).arrayNames = {'M1','PMd'};

%%
datasets(10).filename = 'R_2017-10-12_1';
datasets(10).task = 'bci_vs_arm';
datasets(10).subject = 'Jenkins';
datasets(10).savetags = [15];
datasets(10).controlType = 'arm';
datasets(10).arrayNames = {'M1','PMd'};

datasets(11).filename = 'R_2017-10-12_1';
datasets(11).task = 'bci_vs_arm';
datasets(11).subject = 'Jenkins';
datasets(11).savetags = [6];
datasets(11).controlType = 'bci_gain1';
datasets(11).arrayNames = {'M1','PMd'};

datasets(12).filename = 'R_2017-10-12_1';
datasets(12).task = 'bci_vs_arm';
datasets(12).subject = 'Jenkins';
datasets(12).savetags = [8];
datasets(12).controlType = 'bci_gain2';
datasets(12).arrayNames = {'M1','PMd'};

datasets(13).filename = 'R_2017-10-12_1';
datasets(13).task = 'bci_vs_arm';
datasets(13).subject = 'Jenkins';
datasets(13).savetags = [10];
datasets(13).controlType = 'bci_gain3';
datasets(13).arrayNames = {'M1','PMd'};

datasets(14).filename = 'R_2017-10-12_1';
datasets(14).task = 'bci_vs_arm';
datasets(14).subject = 'Jenkins';
datasets(14).savetags = [11];
datasets(14).controlType = 'bci_gain4';
datasets(14).arrayNames = {'M1','PMd'};

datasets(15).filename = 'R_2017-10-12_1';
datasets(15).task = 'bci_vs_arm';
datasets(15).subject = 'Jenkins';
datasets(15).savetags = [4];
datasets(15).controlType = 'auto';
datasets(15).arrayNames = {'M1','PMd'};

%%
datasets(16).filename = 'R_2017-10-16_1';
datasets(16).task = 'bci_vs_arm_nd';
datasets(16).subject = 'Jenkins';
datasets(16).savetags = [14 16];
datasets(16).controlType = 'arm';
datasets(16).arrayNames = {'M1','PMd'};

datasets(17).filename = 'R_2017-10-16_1';
datasets(17).task = 'bci_vs_arm_nd';
datasets(17).subject = 'Jenkins';
datasets(17).savetags = [7];
datasets(17).controlType = 'bci_gain2';
datasets(17).arrayNames = {'M1','PMd'};

datasets(18).filename = 'R_2017-10-16_1';
datasets(18).task = 'bci_vs_arm_nd';
datasets(18).subject = 'Jenkins';
datasets(18).savetags = [9];
datasets(18).controlType = 'bci_gain3';
datasets(18).arrayNames = {'M1','PMd'};

datasets(19).filename = 'R_2017-10-16_1';
datasets(19).task = 'bci_vs_arm_nd';
datasets(19).subject = 'Jenkins';
datasets(19).savetags = [11];
datasets(19).controlType = 'bci_gain4';
datasets(19).arrayNames = {'M1','PMd'};

datasets(20).filename = 'R_2017-10-16_1';
datasets(20).task = 'bci_vs_arm_nd';
datasets(20).subject = 'Jenkins';
datasets(20).savetags = [1];
datasets(20).controlType = 'auto';
datasets(20).arrayNames = {'M1','PMd'};

datasets(21).filename = 'R_2017-10-16_1';
datasets(21).task = 'bci_vs_arm_nd';
datasets(21).subject = 'Jenkins';
datasets(21).savetags = [5];
datasets(21).controlType = 'bci_gain1';
datasets(21).arrayNames = {'M1','PMd'};

%%
datasets(22).filename = 'R_2017-10-19_1';
datasets(22).task = 'bci_vs_arm_nd';
datasets(22).subject = 'Jenkins';
datasets(22).savetags = [9];
datasets(22).controlType = 'arm';
datasets(22).arrayNames = {'M1','PMd'};

datasets(23).filename = 'R_2017-10-19_1';
datasets(23).task = 'bci_vs_arm_nd';
datasets(23).subject = 'Jenkins';
datasets(23).savetags = [5 7];
datasets(23).controlType = 'bci_gain2';
datasets(23).arrayNames = {'M1','PMd'};

%%
paths = getFRWPaths();

%%
speedMax = 1500;
binMS = 10;

%%
for d=1
    saveableName = [strrep(datasets(d).filename,'.','-') '_' datasets(d).controlType];
    outDir = [paths.dataPath filesep 'Derived' filesep 'BCIDynamicsPredata'];
    
    %%
    %load dataset
    if strcmp(datasets(d).task,'BG_2DCursor')
        sessionPath = [paths.dataPath filesep 'BG Datasets' filesep datasets(d).filename filesep];
        R = getSTanfordBG_RStruct( sessionPath, datasets(d).savetags );
    else      
        if strcmp(datasets(d).task,'3ring')
            fileName = [paths.dataPath filesep 'Monk' filesep datasets(d).subject 'Data' filesep datasets(d).filename '.mat'];
        elseif strcmp(datasets(d).task,'CursorJump')
            fileName = [paths.dataPath filesep 'Monk' filesep 'SergeyCursorJump' filesep 'Data' filesep datasets(d).filename '.mat'];
        elseif strcmp(datasets(d).task,'gain')
            fileName = [paths.dataPath filesep 'Monk' filesep 'SergeyGain' filesep datasets(d).filename '.mat'];
        elseif strcmp(datasets(d).task,'bci_vs_arm') || strcmp(datasets(d).task,'bci_vs_arm_nd')
            fileName = [paths.dataPath filesep 'Monk' filesep 'BCIvsArm' filesep datasets(d).filename '.mat'];
        end
        load(fileName);
    end
    
    %%
    %first remove all trials not belonging to appropriate blocks
    if ~strcmp(datasets(d).task,'BG_2DCursor') && ~isempty(datasets(d).savetags)
        saveTag = zeros(size(R));
        for t=1:length(R)
            saveTag(t) = R(t).startTrialParams.saveTag;
        end
        R = R(ismember(saveTag, datasets(d).savetags));
    end
    
    %%
    %initial formatting
    if strcmp(datasets(d).controlType, 'arm')
        opts.filter = true;
        opts.useDecodeSpeed = false;
    elseif strcmp(datasets(d).controlType, 'auto')
        opts.filter = false;
        opts.useDecodeSpeed = false;
    else
        opts.filter = false;
        opts.useDecodeSpeed = true;
    end
    data = unrollR_1ms( R, opts );
    
    if strcmp(datasets(d).task,'3ring')
        data = format3ring( data );
    elseif strcmp(datasets(d).task,'gain')
        data = formatGain( R, data );
    elseif strcmp(datasets(d).task,'CursorJump')
        data = formatCursorJump( data );
    elseif strcmp(datasets(d).task,'bci_vs_arm') || strcmp(datasets(d).task,'bci_vs_arm_nd')
        data = formatRadial8( data );
    elseif strcmp(datasets(d).task,'BG_2DCursor')
        data.outerTargCodes = [9 8 6 3 1 2 4 7];
        data.isOuterReach = ismember(data.targCodes, [1 2 3 4 6 7 8 9]);
    end
    
    %%
    %basic trial filtering
    nTrials = size(data.trialSeg,1);
    trialFilter = true(nTrials,1);
    trialFilter = trialFilter & data.isOuterReach & data.isSuccessful;
    
    if strcmp(datasets(d).controlType,'arm')
        badSpeed = false(length(data.targCodes),1);
        for t=1:length(badSpeed)
            loopIdx = data.trialSeg(t,1):data.trialSeg(t,2);
            badSpeed(t) = any(data.cursorSpeed(loopIdx)>speedMax);
        end
        trialFilter = trialFilter & ~badSpeed;
    end
    
    if strcmp(datasets(d).task,'gain')
        trialFilter = trialFilter & data.isConstantGain;
    end
    
    if strcmp(datasets(d).task,'3ring') || strcmp(datasets(d).task,'bci_vs_arm')
        %this is a delay dataset, so use only trials with a delay (not
        %non-delay catch trials)
        trialFilter = trialFilter & ~isnan(data.reachEvents(:,1));
    end
    
    %align by: (1) go cue, (2) movement start (via speed threshold), (3) first target
    %entrance (acquire), (4) lfads neural onset
    alignTypes = {'Go','MovStart','TargEnter'};
    alignEvents = nan(size(data.reachEvents,1),3);
    alignEvents(:,1) = data.reachEvents(:,2);
    alignEvents(:,3) = data.reachEvents(:,4);
    
    %%
    %get average peak speed per condition (for thresholding)
    nTargCodes = length(unique(data.targCodes));
    speedProfiles = cell(nTargCodes,1);
    maxSpeed = cell(nTargCodes,1);
    for t=1:size(data.reachEvents,1)
        loopIdx = data.reachEvents(t,2):data.trialSeg(t,2);
        if ~trialFilter(t) || any(loopIdx<0 | loopIdx>length(data.cursorSpeed))
            continue;
        end
        targIdx = data.targCodes(t);
        maxSpeed{targIdx} = [maxSpeed{targIdx}, max(data.cursorSpeed(loopIdx))];
        
        loopIdx = (data.reachEvents(t,2)):(data.reachEvents(t,2)+1000);
        if any(loopIdx<0 | loopIdx>length(data.cursorSpeed))
            continue;
        end
        speedProfiles{targIdx} = [speedProfiles{targIdx}; data.cursorSpeed(loopIdx)'];
    end
    
    meanPeakSpeed = zeros(nTargCodes,1);
    for t=1:nTargCodes
        meanPeakSpeed(t) = mean(maxSpeed{t});
    end
    
    baselineSpeed = zeros(1,2);
    sp = vertcat(speedProfiles{:});
    sp = sp(:,1:100);
    baselineSpeed(1) = mean(sp(:));
    baselineSpeed(2) = std(sp(:));
    
    %%
    %threshold based on *speedPCT peak speed
    if strcmp(datasets(d).controlType,'arm')
        speedPCT = 0.30;
    else
        speedPCT = 0.50;
    end
    for t=1:size(data.reachEvents,1)
        if  ~trialFilter(t)
            continue;
        end
        speedThresh = speedPCT * meanPeakSpeed(data.targCodes(t));
        
        loopIdx = data.reachEvents(t,2):data.trialSeg(t,2);
        startIdx = find(data.cursorSpeed(loopIdx)>speedThresh,1,'first');
        if ~isempty(startIdx)
            alignEvents(t,2) = loopIdx(startIdx);
        end
    end
    
    %reject trials that don't cross the speed threshold
    trialFilter = trialFilter & ~isnan(alignEvents(:,2));
    
    %reject trials that have an outlier speed profile
    speedOutlier = false(size(trialFilter));
    for t=1:length(speedOutlier)
        loopIdx = data.reachEvents(t,2):(data.reachEvents(t,2)+1000);
        if any(loopIdx>length(data.cursorSpeed)) || ~trialFilter(t)
            continue;
        end
        
        speedTraj = data.cursorSpeed(loopIdx)';
        mnSpeedTraj = mean(speedProfiles{data.targCodes(t)});
        stdSpeedTraj = std(speedProfiles{data.targCodes(t)});
        
        speedOutlier(t) = any(speedTraj < (mnSpeedTraj - stdSpeedTraj*5) | ...
            speedTraj > (mnSpeedTraj + stdSpeedTraj*5));
    end
    
    %%
    %pull correct set of conditions for this dataset
    if strcmp(datasets(d).task,'3ring')
        conSet = data.outerRingCodes(1:2:end);
    elseif strcmp(datasets(d).task,'gain')
        conSet = data.outerLowGainCodes;
    elseif strcmp(datasets(d).task,'CursorJump') || strcmp(datasets(d).task,'bci_vs_arm') || strcmp(datasets(d).task,'bci_vs_arm_nd')
        conSet = data.outerTargCodes;
    elseif strcmp(datasets(d).task,'BG_2DCursor')
        conSet = data.outerTargCodes;
    end
    
    %%
    %confirm alignment
    plotCon = conSet(1);
    trlIdx = find(data.targCodes==plotCon);
    speedConcat = cell(3,1);
    
    figure('Position',[680   823   914   275]);
    for alignIdx=1:3
        subplot(1,3,alignIdx);
        hold on;
        for t=1:length(trlIdx)
            if ~trialFilter(trlIdx(t))
                continue;
            end
            loopIdx = (alignEvents(trlIdx(t), alignIdx)-200):(alignEvents(trlIdx(t), alignIdx)+1200);
            if loopIdx(1)<0 || loopIdx(end)>length(data.cursorSpeed)
                continue;
            end
            plot(data.cursorSpeed(loopIdx),'LineWidth',2);
            speedConcat{alignIdx} = [speedConcat{alignIdx}; data.cursorSpeed(loopIdx)'];
        end
        title(alignTypes{alignIdx});
        set(gca,'LineWidth',1.5,'FontSize',16);
    end
    set(gcf,'PaperPositionMode','auto','InvertHardcopy','off','Color','w');
    saveas(gcf,[outDir filesep saveableName '_speedAlign.png'],'png');
    saveas(gcf,[outDir filesep saveableName '_speedAlign.fig'],'fig');
    
    %%
    %get trial average speed profile
    avgSpeed = mean(speedConcat{2});
    plotIdx = 1:700;
    
    figure
    hold on
    for s=1:size(speedConcat{2},1)
        plot(plotIdx-1, speedConcat{2}(s,plotIdx));
    end
    plot(avgSpeed(plotIdx),'-k','LineWidth',2);
    set(gca,'LineWidth',1.5,'FontSize',16);
    xlabel('Time (ms)');
    ylabel('Speed (mm/s)');
    
    adjustSpeed = avgSpeed;
    adjustSpeed = adjustSpeed - 55;
    adjustSpeed(adjustSpeed<0) = 0;
    adjustSpeed = adjustSpeed*1.25;
    
    movTime = 360;
    tAxis = linspace(0,movTime,movTime);
    minJerk = 80*(10*(tAxis/movTime).^3-15*(tAxis/movTime).^4+6*(tAxis/movTime).^5);
    minJerk = minJerk';
    minJerkVel = [0; diff(minJerk)*1000];
    minJerkVel = [zeros(150,1); minJerkVel; zeros(200,1)];
    minJerkPos = cumsum(minJerkVel)/1000;
    timeAxisMS = 0:(length(minJerkVel)-1);
    
    figure
    hold on;
    plot(adjustSpeed(plotIdx),'LineWidth',2);
    plot(minJerkVel,'LineWidth',2);
    plot(avgSpeed(plotIdx),'-k','LineWidth',2);
    set(gca,'LineWidth',1.5,'FontSize',16);
    xlabel('Time (ms)');
    ylabel('Speed (mm/s)');
    
    figure
    hold on
    for s=1:size(speedConcat{2},1)
        plot(plotIdx-1, speedConcat{2}(s,plotIdx));
    end
    plot(minJerkVel,'-k','LineWidth',2);
    set(gca,'LineWidth',1.5,'FontSize',16);
    xlabel('Time (ms)');
    ylabel('Speed (mm/s)');
    %%
    acq = [R(trialFilter).timeFirstTargetAcquire]';
    rt = alignEvents(trialFilter,2)-data.trialSeg(trialFilter,1)+1;
    
    figure; 
    subplot(1,2,1);
    hist(acq-rt,100);
    title('Acq-rt');
    
    subplot(1,2,2);
    hist(rt,100);
    title('rt');
    saveas(gcf,[outDir filesep saveableName '_rtHist.png'],'png');
    saveas(gcf,[outDir filesep saveableName '_rtHist.fig'],'fig');
    
    %%
    %bin neural data and create a neural data and kinematics trial matrix
    timeWindows = {[-499, 1300],[-699, 1100],[-1199, 2000]};
    
    allNeural = cell(length(alignTypes),2);
    allKin = cell(length(alignTypes),1);
    allCon = cell(length(alignTypes),1);
    
    for alignIdx = 1:length(alignTypes)
        allCon{alignIdx} = [];
        timeWindow = timeWindows{alignIdx};
        nBins = (timeWindow(2)-timeWindow(1)+1)/binMS;
        for c=1:length(conSet)
            trlIdx = find(data.targCodes==conSet(c) & trialFilter);
            nTrials = length(trlIdx);
            
            allCon{alignIdx} = [allCon{alignIdx}; repmat(c,nTrials,1)];
            newNeural = cell(2,1); 
            newNeural{1} = nan(nTrials, nBins, 96);
            newNeural{2} = nan(nTrials, nBins, 96);
            newKin = nan(nTrials, nBins, 7);
            for t=1:length(trlIdx)
                loopIdx = (alignEvents(trlIdx(t), alignIdx)+timeWindow(1)):(alignEvents(trlIdx(t), alignIdx)+timeWindow(2));
                if any(loopIdx<1 | loopIdx>length(data.cursorSpeed))
                    continue;
                end
                
                for arrayIdx=1:2
                    if ~isfield(data,['array' num2str(arrayIdx)])
                        continue;
                    end
                    tmp = data.(['array' num2str(arrayIdx)])(loopIdx,:);
                    tmpBinned = zeros(nBins, 96);
                    binIdx = 1:binMS;
                    for b=1:nBins
                        tmpBinned(b,:) = sum(tmp(binIdx,:))*(1000/binMS);
                        binIdx = binIdx + binMS;
                    end
                    
                    newNeural{arrayIdx}(t,:,:) = tmpBinned;
                end
                
                tmp = [data.cursorPos(loopIdx,1:2), data.cursorVel(loopIdx,1:2), data.cursorSpeed(loopIdx), data.targetPos(loopIdx,1:2)];
                tmpBinned = tmp(1:binMS:end,:);
                newKin(t,:,:) = tmpBinned;
            end %trials
            
            allNeural{alignIdx,1} = [allNeural{alignIdx,1}; newNeural{1}];
            allNeural{alignIdx,2} = [allNeural{alignIdx,2}; newNeural{2}];
            allKin{alignIdx} = [allKin{alignIdx}; newKin];
        end %conditions
    end %alignment types
    
    %remove NAN trials
    for alignIdx=1:length(alignTypes)
        hasNan = false(size(allKin{alignIdx},1),1);
        for t=1:size(allKin{alignIdx},1)
            hasNan(t) = any(any(isnan(allKin{alignIdx}(t,:,:))));
        end
        allKin{alignIdx}(hasNan,:,:) = [];
        allNeural{alignIdx,1}(hasNan,:,:) = [];
        allNeural{alignIdx,2}(hasNan,:,:) = [];
        allCon{alignIdx}(hasNan) = [];
    end
    
    neuralAvg = cell(length(alignTypes),2);
    kinAvg = cell(length(alignTypes),1);    
    for alignIdx = 1:length(alignTypes)
        neuralAvg{alignIdx,1} = zeros(length(conSet),nBins,96);
        neuralAvg{alignIdx,2} = zeros(length(conSet),nBins,96);
        kinAvg{alignIdx,1} = zeros(length(conSet),nBins,7);
        
        for c=1:length(conSet)
            trlIdx = find(allCon{alignIdx}==c);
            for arrayIdx=1:2
                neuralAvg{alignIdx,arrayIdx}(c,:,:) = squeeze(mean(allNeural{alignIdx,arrayIdx}(trlIdx,:,:)));
            end
            
            tmp = allKin{alignIdx}(trlIdx,:,:);
            tmp(tmp>speedMax) = NaN;
            kinAvg{alignIdx}(c,:,:) = squeeze(nanmean(tmp));
        end
    end
    
    %%
    %confirm neural alignment
    timeAxis = cell(size(timeWindows));
    for t=1:length(timeAxis)
        timeAxis{t} = timeWindows{t}(1):binMS:timeWindows{t}(2);
        timeAxis{t} = timeAxis{t} + (binMS/2);
        timeAxis{t} = timeAxis{t}/1000;
    end
    
    figure
    for alignIdx=1:length(alignTypes)
        for arrayIdx=1:2
            subplot(2,3,(arrayIdx-1)*length(alignTypes) + alignIdx)
            plot(timeAxis{alignIdx}, mean(squeeze(neuralAvg{alignIdx,arrayIdx}(1,:,:))'),'LineWidth',2);
            title(alignTypes{alignIdx});
            set(gca,'LineWidth',1.5,'FontSize',16);
            xlim([timeAxis{alignIdx}(1), timeAxis{alignIdx}(end)]);
        end
    end
    set(gcf,'PaperPositionMode','auto','InvertHardcopy','off','Color','w');
    saveas(gcf,[outDir filesep saveableName '_neuralAlign.png'],'png');
    
    %%
    %confirm target codes with velocity averages
    figure
    for alignIdx=1:length(alignTypes)
        subplot(1,3,alignIdx);
        hold on;
        for c=1:length(conSet)
            plot(timeAxis{alignIdx}, squeeze(kinAvg{alignIdx}(c,:,3))','LineWidth',2);
        end
        title(alignTypes{alignIdx});
        set(gca,'LineWidth',1.5,'FontSize',16);
        xlim([timeAxis{alignIdx}(1), timeAxis{alignIdx}(end)]);
    end
    legend({'1','2','3','4','5','6','7','8'});
    set(gcf,'PaperPositionMode','auto','InvertHardcopy','off','Color','w');
    saveas(gcf,[outDir filesep saveableName '_xVel.png'],'png');
    
    %%
    %save data
    metaData = datasets(d);
    outPath = [outDir filesep saveableName '.mat'];
    
    mkdir(outDir);
    save(outPath, 'allNeural','allKin','allCon','neuralAvg','kinAvg','binMS',...
        'timeWindows','timeAxis','metaData','speedMax','speedPCT','alignTypes');
    
    close all;
end
